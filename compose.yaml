services:
  bot:
    build:
      context: .
      dockerfile: cmd/bot/Dockerfile
    env_file:
      - .env
    depends_on:
      - scrapper

  scrapper:
    build:
      context: .
      dockerfile: cmd/scrapper/Dockerfile
    env_file:
      - .env

networks:
  app-network:
    driver: bridge