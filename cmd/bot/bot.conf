telegram = {
    token = ${APP_TELEGRAM_TOKEN}
    api-url = "https://api.telegram.org/bot"
    timeout = 10 s
}

logger = {
    level = INFO
    level = ${?BOT_LOG_LEVEL}
    file = ${?BOT_LOGS_FILE}
}

scrapper-service = {
    url = ${SCRAPPER_URL}
    protocol = "grpc"
    protocol = ${?SCRAPPER_API_PROTOCOL}
}

database = {
    url = ${DATABASE_URL}
    access-type = SQL
    access-type = ${?DATABASE_ACCESS_TYPE}
}

use-queue = ${?USE_QUEUE}
kafka = {
    brokers = ["kafka-1:9092", "kafka-2:9092", "kafka-3:9092"]
    brokers = ${?KAFKA_BROKERS}
    topic = "link-updates"
    topic = ${?KAFKA_TOPIC}
    dlq-topic = "link-updates-dlq"
    dlq-topic = ${?KAFKA_DLQ_TOPIC}
    consumer-group = "bot-group-1"
    consumer-group = ${?KAFKA_BOT_CONSUMER_GROUP}
    retries = 3
    retries = ${?KAFKA_RETRIES}
}

server = {
    protocol = ${?BOT_API_PROTOCOL}
    port = ${?BOT_API_PORT}
}